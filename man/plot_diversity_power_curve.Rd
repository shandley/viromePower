% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_diversity_power_curve.R
\name{plot_diversity_power_curve}
\alias{plot_diversity_power_curve}
\title{Plot Viral Diversity Power Curve}
\usage{
plot_diversity_power_curve(
  param_range,
  param_type = "n_samples",
  effect_size = 1,
  n_samples = 15,
  n_viruses = 200,
  diversity_measure = "shannon",
  alpha = 0.05,
  sparsity = 0.8,
  dispersion = 2,
  n_sim = 50,
  show_individual_sims = FALSE
)
}
\arguments{
\item{param_range}{Vector of parameter values to test (sample sizes or effect sizes)}

\item{param_type}{Type of parameter being varied: "n_samples" or "effect_size" (default: "n_samples")}

\item{effect_size}{Effect size to use when varying sample size (default: 1.0)}

\item{n_samples}{Number of samples to use when varying effect size (default: 15)}

\item{n_viruses}{Number of viral taxa to simulate (default: 200)}

\item{diversity_measure}{Type of diversity to analyze: "shannon", "simpson", "richness", "evenness", "chao1", "ace", "inv_simpson", "fisher_alpha", "goods_coverage", "berger_parker", "bray", "jaccard", or "unifrac" (default: "shannon")}

\item{alpha}{Significance level (default: 0.05)}

\item{sparsity}{Proportion of zeros in the data (default: 0.8)}

\item{dispersion}{Dispersion parameter for viral abundance (default: 2)}

\item{n_sim}{Number of simulations for each point (default: 50)}

\item{show_individual_sims}{Logical indicating whether to show individual simulation points (default: FALSE)}
}
\value{
A ggplot object with the power curve visualization
}
\description{
Creates a power curve visualization for viral diversity analysis, showing how power
changes with sample size or effect size.
}
\examples{
# Power curve for varying sample sizes with Shannon diversity
power_curve <- plot_diversity_power_curve(param_range = seq(5, 30, by = 5), 
                                         diversity_measure = "shannon")

# Power curve for varying sample sizes with Chao1 richness
chao1_curve <- plot_diversity_power_curve(param_range = seq(5, 30, by = 5), 
                                        diversity_measure = "chao1")
                                         
# Power curve for varying effect sizes with Fisher's Alpha
fisher_curve <- plot_diversity_power_curve(param_range = seq(0.5, 2.0, by = 0.3),
                                         param_type = "effect_size",
                                         n_samples = 15,
                                         diversity_measure = "fisher_alpha")

# Power curve for varying sample sizes with Good's Coverage
coverage_curve <- plot_diversity_power_curve(param_range = seq(5, 30, by = 5),
                                          diversity_measure = "goods_coverage")
                                         
# Power curve for varying effect sizes with Bray-Curtis dissimilarity
effect_curve <- plot_diversity_power_curve(param_range = seq(0.05, 0.3, by = 0.05),
                                          param_type = "effect_size",
                                          n_samples = 20,
                                          diversity_measure = "bray")
}
