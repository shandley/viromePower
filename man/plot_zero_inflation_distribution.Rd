% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_zero_inflated_models.R
\name{plot_zero_inflation_distribution}
\alias{plot_zero_inflation_distribution}
\title{Plot Zero-Inflation Parameter Distribution}
\usage{
plot_zero_inflation_distribution(
  zinb_model_fit,
  by_taxa = FALSE,
  group_factor = NULL,
  highlight_threshold = NULL
)
}
\arguments{
\item{zinb_model_fit}{A fitted zero-inflated model object or a list containing
estimated zero-inflation parameters.}

\item{by_taxa}{Logical indicating whether to show distribution by taxa (TRUE) or
by samples (FALSE, default).}

\item{group_factor}{Optional factor indicating group membership for samples or taxa.
If provided, distributions will be faceted by group.}

\item{highlight_threshold}{Optional numeric value. Zero-inflation parameters
above this threshold will be highlighted.}
}
\value{
A ggplot2 object visualizing the distribution of zero-inflation parameters.
}
\description{
Creates visualizations of the zero-inflation parameter distribution across samples or taxa.
}
\examples{
\dontrun{
# Simulate zero-inflated data
sim_data <- simulate_zero_inflated_virome(
  n_samples = 50,
  n_taxa = 100,
  mu = 10,
  size = 0.5,
  zi_prob = 0.3,
  group_effect = 1.5,
  group_factor = rep(c("A", "B"), each = 25)
)

# Assume we've fit a ZINB model and have parameter estimates
zi_params <- list(
  pi = matrix(rbeta(50*100, 2, 5), nrow = 50),  # Example parameter estimates
  groups = sim_data$groups,
  taxa_names = paste0("Taxon_", 1:100)
)

# Plot zero-inflation distribution
plot_zero_inflation_distribution(
  zinb_model_fit = zi_params,
  by_taxa = FALSE,
  group_factor = sim_data$groups,
  highlight_threshold = 0.5
)
}

}
